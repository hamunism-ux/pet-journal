# Netlify 讀這個檔案就知道怎麼建置，通常不用改
[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

# 單頁應用：所有路徑都交給 index.html（登入連結回來時需要）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
