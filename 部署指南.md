# 寵物手帳 雲端版 · 最短部署流程

**第一次：做 3 件事，約 10 分鐘。之後每次更新：做 1 件事，約 1 分鐘。**
不用在電腦上安裝任何東西。

---

## 第一次

### ① Supabase（建資料庫，一次就好）

1. https://supabase.com 用 GitHub 登入 → **New project** → 名稱隨意、密碼隨意設一個、Region 選 **Singapore** → Create。等 1 分鐘。
2. 左邊 **SQL Editor** → **New query** → 把 `supabase/schema.sql` 整份貼進去 → **Run**。看到 Success 就對了。
3. 左邊 **Authentication** → **Sign In / Providers** → 找到 **Allow anonymous sign-ins** → 打開 → Save。
4. 左邊 **Project Settings** → **API** → 把 **Project URL** 和 **anon public** 這兩個值複製下來。

### ② 把兩個值填進去

解壓縮 zip，用任何文字編輯器（記事本、TextEdit 都行）打開 `src/config.js`，把剛剛複製的兩個值貼進引號裡：

```js
export const SUPABASE_URL = "https://xxxxxxxx.supabase.co";
export const SUPABASE_ANON_KEY = "eyJ...很長一串...";
```

存檔。整份專案只有這個檔案需要你改。

### ③ 上傳到 GitHub

到你的 GitHub 倉庫 → **Add file → Upload files** → 把解壓縮後資料夾裡的**全部內容**拖進去（不是拖資料夾本身，是打開資料夾後全選拖進去）→ 下面按 **Commit changes**。

Netlify 會自動開始建置，1–2 分鐘後你的網址就是新版了。

**打開網址：不用登入，直接就能新增寵物。** 到 Supabase 左邊 **Table Editor → pets** 看到一列，就完成了。

---

## 之後每次更新

我給你新的 zip → 解壓縮 → GitHub **Upload files** 拖進去 → **Commit**。就這樣。

`src/config.js` 已經填好的話，新 zip 裡的 `config.js` 是空白範本，**上傳時把它排除**（或上傳後再填一次）。最省事的做法：大多數更新只有 `src/App.jsx` 一個檔案變，你就只拖那一個。

---

## 想開 AI 功能才需要（可以先不做）

讀成分表、讀條碼數字、猜品種這三個功能要 Anthropic 金鑰。沒設的話按鈕會顯示「讀取失敗」，其他功能都正常。

Netlify → 你的網站 → **Site configuration → Environment variables → Add a variable**：
Key 填 `ANTHROPIC_API_KEY`，Value 填你的金鑰。存了之後 **Deploys → Trigger deploy**（改變數要手動重建）。

---

## 卡住看這裡

| 看到什麼 | 怎麼辦 |
|---|---|
| 「還沒填資料庫連線」 | `src/config.js` 沒填或沒上傳到 |
| 「自動登入失敗」 | ①-3 那個開關沒打開 |
| 儲存後首頁出現黃色提醒 | ①-2 的 SQL 沒跑完整，回去再 Run 一次（可以重複跑） |
| Netlify 建置失敗 | 檔案沒傳齊，看 deploy log 紅字寫缺什麼 |

---

## 現在這個版本的取捨，你要知道

**訪客模式**：打開網頁自動有一個帳號，綁在那個瀏覽器上。同一支手機再開，寵物都在；**換手機或清瀏覽器資料就找不回來**。這是刻意的：驗證功能階段，「零輸入就能用」比「跨裝置同步」重要。

要換成 Email 登入時，改 `src/config.js` 最後一行 `AUTH_MODE = "email"`，再到 Supabase **Authentication → URL Configuration** 把 Site URL 填成你的 Netlify 網址。登入畫面程式已經寫好在裡面了。

**你看得到所有人的資料**：Supabase **Table Editor → pets**。每個使用者只看得到自己的，你在後台看全部。

**兩個不要**：Supabase API 頁那個 `service_role` key 不要放進任何檔案；Anthropic 金鑰只放 Netlify 環境變數。
