# 寵物手帳 雲端版 · 部署指南

寫給第一次做這件事的人。照順序做，每一步都有「怎麼確認做對了」。全部大約 40 分鐘，不需要在自己電腦上安裝任何程式。

---

## 0. 先搞懂你手上有什麼

這個資料夾就是一個完整的網站專案。三個地方各管一件事：

| 誰 | 管什麼 | 你要做什麼 |
|---|---|---|
| **Supabase** | 資料庫＋登入 | 開一個專案、貼一段 SQL、抄兩個值 |
| **GitHub** | 放程式碼 | 把這個資料夾的檔案放進你現有的倉庫 |
| **Netlify** | 把程式碼變成網站、跑後端 | 填三個環境變數 |

資料流：使用者在網頁登入 → 新增寵物 → 存進 Supabase 的 `pets` 資料表（每一列都記著是誰的） → 你在 Supabase 後台可以看到所有人的資料。

資料夾裡的檔案，你需要認得的只有這幾個：

```
supabase/schema.sql          ← 資料表和權限規則，貼到 Supabase 用
netlify/functions/vision.mjs ← 後端：把照片交給 AI（金鑰藏在這裡）
src/App.jsx                  ← 前端：整個 app 的畫面和邏輯
src/lib/supabase.js          ← 前端連 Supabase 的設定
src/lib/db.js                ← 前端讀寫資料表的程式
netlify.toml / package.json  ← 告訴 Netlify 怎麼建置，不用改
```

---

## 1. Supabase：建資料庫（約 15 分鐘）

### 1-1 開專案
1. 到 https://supabase.com 註冊（用 GitHub 帳號登入最快）。
2. **New project**。名稱隨意（例如 `pet-journal`），**Database Password** 設一個強密碼並記下來（之後很少用到，但要收好），**Region** 選離使用者最近的（在台灣或新加坡就選 Singapore）。
3. 等 1–2 分鐘專案建好。

### 1-2 建資料表和門鎖
1. 左邊選單 **SQL Editor** → **New query**。
2. 打開 `supabase/schema.sql`，整份複製貼上，按 **Run**。
3. 看到 `Success. No rows returned` 就對了。

**確認**：左邊 **Table Editor** 應該出現 `pets` 這張表，右上角有 **RLS enabled** 的綠色標記。左邊 **Authentication → Policies** 找 `pets`，應該有四條規則（read / insert / update / delete）。這四條就是交接文件說要「人工檢查」的東西：每一條都寫著 `owner_id = auth.uid()`，意思是「只有本人能碰自己的那幾列」。

### 1-3 設定登入
1. 左邊 **Authentication → Providers**，確認 **Email** 是開著的（預設開）。
2. 左邊 **Authentication → URL Configuration**：
   - **Site URL** 填你的 Netlify 網址，例如 `https://你的站名.netlify.app`（沒有結尾斜線）
   - **Redirect URLs** 按 Add URL，填同一個網址
   
   這一步是最多人漏掉的。沒填的話，使用者點了信裡的登入連結會被帶到 `localhost:3000`，然後什麼都沒發生。

### 1-4 抄兩個值
左邊 **Project Settings → API**：
- **Project URL**（長得像 `https://abcdefgh.supabase.co`）
- **anon public** key（很長一串 `eyJ...`）

先貼到記事本，第 3 步要用。

> anon key 是設計來放在前端的，公開沒關係，因為真正的門鎖是 RLS。
> 同一頁下面還有一個 **service_role** key，那個**絕對不能**放進任何前端檔案或 GitHub。這份專案完全不需要它。

---

## 2. GitHub：把程式放上去（約 10 分鐘）

你已經有 `pet-passport` 倉庫在跑 Netlify 自動部署，所以只要換掉裡面的檔案。

**最簡單的方式（不用裝任何東西）**：
1. 在電腦上解壓縮 `pet-journal-cloud.zip`。
2. 到 GitHub 打開你的倉庫，把舊檔案先刪掉：每個檔案點進去 → 右上角垃圾桶 → Commit。（或者更省事：開一個**新倉庫** `pet-journal`，之後在 Netlify 改連到新倉庫。）
3. **Add file → Upload files**，把解壓縮後資料夾裡的**所有東西**拖進去（包含 `src`、`netlify`、`supabase` 資料夾和根目錄的檔案）。
4. 下面填一句說明（例如 `v3.0 cloud`），**Commit changes**。

**確認**：倉庫首頁看得到 `package.json`、`netlify.toml`、`src/App.jsx`、`netlify/functions/vision.mjs`。

> 不要上傳 `node_modules` 資料夾（zip 裡本來就沒有）。`.env.example` 可以上傳，它只是範本；真正的 `.env` 永遠不要上傳。

---

## 3. Netlify：填變數、部署（約 10 分鐘）

1. 到 Netlify，打開你的網站 → **Site configuration → Environment variables** → **Add a variable**，加三個：

   | Key | Value |
   |---|---|
   | `VITE_SUPABASE_URL` | 第 1-4 步的 Project URL |
   | `VITE_SUPABASE_ANON_KEY` | 第 1-4 步的 anon public key |
   | `ANTHROPIC_API_KEY` | 你的 Anthropic 金鑰（console.anthropic.com 申請） |

   名字要一模一樣，大小寫也是。

2. **Site configuration → Build & deploy → Build settings** 確認：Build command 是 `npm run build`，Publish directory 是 `dist`。`netlify.toml` 已經寫了，Netlify 通常會自己讀到；如果畫面上是空的或不一樣，手動填。

3. **Deploys → Trigger deploy → Deploy site**。等 1–2 分鐘。

**確認**：Deploy 狀態變 **Published**。點進 deploy log，最後幾行應該有 `✓ built in ...` 和 `Functions bundling: vision`。

---

## 4. 實際測一遍

1. 用手機開你的 Netlify 網址。應該看到登入頁。
2. 輸入 Email → 寄登入連結 → 到信箱點連結 → 回到網站，看到空的手帳。
3. 新增一隻寵物，儲存。
4. 到 Supabase **Table Editor → pets**，應該多了一列，`owner_id` 是一串 uuid。
5. 換一台裝置（或無痕視窗）登入同一個 Email，剛剛那隻寵物應該在。
6. 進寵物頁 → 檢查商品 → 拍成分表，AI 能讀出來就代表後端 Function 也通了。

六步都過，就部署完成了。

---

## 5. 出問題時看這裡

| 現象 | 原因 | 怎麼修 |
|---|---|---|
| 開網頁顯示「還沒設定資料庫連線」 | Netlify 環境變數沒填或名字打錯 | 回第 3 步檢查，改完要**重新 Trigger deploy**（改變數不會自動重建） |
| 點信裡的連結跑到 localhost | Supabase 的 Site URL 沒設 | 回第 1-3 步 |
| 收不到登入信 | 免費方案每小時有寄信上限（很低）；或進垃圾郵件 | 等一小時再試；正式上線前要在 Supabase 設自己的 SMTP |
| 儲存後首頁出現黃色提醒「資料暫時無法儲存」 | RLS 擋掉了，通常是 schema.sql 沒跑完整 | 回第 1-2 步重跑一次（可以重複執行） |
| AI 讀取失敗 | `ANTHROPIC_API_KEY` 沒設（後端回 503）或沒登入（401） | 檢查變數；Netlify **Logs → Functions → vision** 看錯誤訊息 |
| iPhone 拍條碼讀不到 | 照片太糊或反光 | 讓條碼佔滿畫面、對焦；程式會自動退到 AI 讀數字、再退到手動 |
| Deploy 失敗 | 通常是檔案沒傳齊 | 看 deploy log 紅字，多半寫著缺哪個檔案 |

---

## 6. 之後怎麼看「所有人的資料」

這正是你這次想做的事。RLS 只擋 app 端，你身為專案擁有者在 Supabase 後台看得到全部：

- **Table Editor → pets**：直接翻表，可以篩選、排序、匯出 CSV。
- **SQL Editor**：例如看物種分布
  ```sql
  select species, count(*) from pets group by species;
  ```
  或看最常見的過敏原
  ```sql
  select unnest(allergies) as allergen, count(*) from pets group by 1 order by 2 desc;
  ```

之後要做「使用者拍成分表累積真實商品資料」，就是再開一張 `products` 表，同樣的套路。

---

## 7. 三條安全底線

1. `service_role` key 永遠不進前端、不進 GitHub。
2. `ANTHROPIC_API_KEY` 只放 Netlify 環境變數。後端 Function 已經加了「必須登入才能用」的檢查，避免別人拿你的額度。
3. 改資料表結構一律走 SQL Editor，改完到 Policies 頁看一眼四條規則還在不在。

---

## 8. 下一步

雲端和登入做完之後，交接文件的優先順序接著是：**健康紀錄列表**（多筆疫苗／驅蟲／看診／體重）。那會是第二張資料表 `health_records`，做法和這次一樣：schema.sql 加表、db.js 加讀寫、App.jsx 加畫面。

照片目前是縮小後直接存在資料表裡，50 隻以內沒問題；使用者多了再改用 Supabase Storage，不用現在煩惱。
